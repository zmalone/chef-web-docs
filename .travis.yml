#before_install:
   #- sudo apt-get update -qq
   #- sudo apt-get install -qq linkchecker
cache:
  # - apt
  - bundler
deploy:
  - provider: s3
    access_key_id: 'abcd'
    secret_access_key: 'efgh'
    bucket: preprod.learnchef.com
    skip_cleanup: true
    local-dir: build
    on:
      branch: nls/travis
install: bundle install --deployment --without development
script: bundle exec middleman build
