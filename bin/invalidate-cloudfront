#!/bin/sh
if [ "$TRAVIS" ] && [ "$TRAVIS_BRANCH" == "release" ]; then
  bundle exec middleman invalidate
else
  echo "Not running in Travis CI and not on the release branch. Skipping CloudFront invalidation."
fi
