---
- question: "In Test Kitchen, how do you assign an IP address to an instance?"
  answer: 0
  choices:
    - Add the IP address to the `driver` section of the .kitchen.yml file.
    - Add the IP address to the metadata.rb file.
    - Include the IP address in the default recipe.
- question: "What is the purpose of the lamp cookbook?"
  answer: 2
  choices:
    - It creates the stack required by the web application.
    - It creates an Ubuntu instance.
    - It creates a general purpose LAMP stack.
