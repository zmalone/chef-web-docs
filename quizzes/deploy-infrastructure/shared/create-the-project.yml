---
- question: "A Chef Automate pipeline:"
  answer: 2
  choices:
    - Is another term for a chef-client run.
    - Is a way to review what other teams are doing.
    - Is a series of quality gates.
- question: "You determine what happens in a pipeline phase with:"
  answer: 2
  choices:
    - A configuration file.
    - A .kitchen.yml file.
    - A build cookbook.
- question: "The `delivery init` command:"
  answer: 1
  choices:
    - Starts a run of the pipeline.
    - Creates a Chef Automate project and a pipeline.
    - Creates a basic build cookbook.
- question: "Each recipe in a build cookbook corresponds to a:"
  answer: 1
  choices:
    - Pipeline stage.
    - Pipeline phase.
    - Runner.
- question: "Which file controls the behavior of the build cookbook?"
  answer: 2
  choices:
    - metadata.rb.
    - config.rb.
    - config.json.
- question: "The goal of the Acceptance stage is to:"
  answer: 2
  choices:
    - Run integration tests.
    - Run smoke tests.
    - Allow teams to decide if a change should go to production.
