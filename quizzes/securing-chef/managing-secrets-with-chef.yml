---
- question: "When is it acceptable to store secrets in cookbooks?"
  answer: 2
  choices:
    - When they are encrypted with the `openssl` command.
    - If the cookbook is not open source.
    - Never.
- question: "A data bag:"
  answer: 1
  choices:
    - Stores encrypted data.
    - Is stored as JSON data.
    - Is stored on the node.
- question: "Encrypted data bags use:"
  answer: 1
  choices:
    - Asymmetric encryption.
    - Shared secret encryption.
    - PKI.
- question: "The problem with using a shared key is:"
  answer: 0
  choices:
    - You're responsible for distributing keys.
    - A shared key is easily stolen.
    - Only one party needs access to the secret key.
- question: "The chef-vault gem:"
  answer: 2
  choices:
    - Uses symmetric encryption to encrypt data bag items.
    - Uses OpenSSL to encrypt data bag items.
    - Uses public keys to encrypt data bag items.
- question: "The chef-vault gem:"
  answer: 1
  choices:
    - Generates a shared secret.
    - Generates a shared secret and a hash of that secret.
    - Generates a data bag.
- question: "The chef-vault gem:"
  answer: 0
  choices:
    - Lets you control which people and nodes can decrypt the data bag item.
    - Lets you audit who has accessed a system.
    - Can be completely automated.
- question: "An advantage to using chef-vault is:"
  answer: 1
  choices:
    - There is a GUI that makes it easy to use.
    - It relies on keys that are already on the systems so you don't have to manage key redistribution.
    - It places the vault on the nodes so only they can access it.
