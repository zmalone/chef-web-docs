---
title: 'Install and configure Chef server'
order: 2
keywords: TODO
layout: lesson
sections: [1, 2, 3]
next: {heading: Next, partial: next}
time_to_complete: 60 minutes
---
Now that you're performed the prerequisite tasks, you're ready to install and configure Chef server on your system.

[COMMENT] We use `sudo` to highlight where root access is required. You can omit `sudo` from the commands if you're running as the root user.
