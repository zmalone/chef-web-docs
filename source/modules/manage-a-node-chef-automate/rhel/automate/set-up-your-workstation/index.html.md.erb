---
title: 'Set up your workstation'
order: 1
description: "Install the Chef DK and a text editor on your workstation."
time_to_complete: 20 minutes
snippet_path: manage-a-node/rhel/aws-automate/set-up-your-workstation
snippet_workstation: ubuntu
headings: [
  { label: '1. Install the Chef DK', href: '#step1' },
  { label: '2. Open a command prompt', href: '#step2' },
  { label: '3. Verify your Chef DK installation', href: '#step3' },
  { label: '4. Set up your text editor', href: '#step4' },
  { label: '5. Create a working directory', href: '#step5' },
  { label: '6. Install Git', href: '#step6' },
  { label: '7. Verify your SSH client', href: '#step7' }
  ]
quiz:
  - question: In Chef terms, what is a node?
    answer: 1
    choices:
      - The system from which you administer your Chef server.
      - Any computer that's managed by a Chef server.
      - Any virtual machine or cloud instance, whether it is managed by a Chef server or not.
  - question: Which of the following happens during the bootstrap process?
    answer: 0
    choices:
      - The node registers instead with the Chef server, downloads the latest cookbooks in the run-list, and then runs `chef-client`.
      - The node registers instead with the Chef server, downloads the latest cookbooks in the run-list, but does not run `chef-client`.
      - The node registers instead with the Chef server, but does not download cookbooks or run `chef-client`.
  - question: True or False - The bootstrap process supports both key-based and password authentication.
    answer: 0
    choices:
      - "True"
      - "False"
---
<%= partial 'modules/manage-a-node/shared/set-up-your-workstation/index' %>
