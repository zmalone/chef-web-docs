---
description: "Learn how treating your <i>infrastructure</i> as <i>code</i> with Chef simplifies the way you manage and automate your infrastructure, making it more maintainable, versionable, testable, and collaborative."
layout: content
title: "Tutorials"
type: tutorials
---
#topics
  - @topic_pages = current_page.siblings.reject { |topic| topic.no_index? }.select{|topic| topic.data.type && topic.data.type == "topic"}
  - [{:category => 'getting-started', :header => 'Get started with Chef'},
     {:category => 'local-development', :header => "Develop and test your infrastructure code locally"},
     {:category => 'manage-and-premium-features', :header => 'Manage your own Chef server'},
     {:category => 'chef-automate', :header => 'Ship applications and infrastructure with Chef Automate'}].each do |options|
    = partial 'layouts/tutorial-section', locals: { header: options[:header], topics: @topic_pages.select{|topic| topic.data.category == options[:category]} }
